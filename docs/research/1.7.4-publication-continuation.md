# Authorized publication and 2.0 continuation

The user explicitly authorized the remaining Apple submission/native Mac test, all draft uploads, PR merge, publication when fully ready, then pulling main and integrating into 2.0. Preserve the user's edited release notes. Do not ask for those permissions again.

## Current state (September 22)

- Main checkout: `/Users/cdevroe/Sites/projects/signboard`, branch `codex/release-1.7.4`. PR #60 remains draft. The Mac/Windows/Linux release build is `20260921.1`, source fingerprint `87088b1449418b3efcde5b96798e95735ab8f991ea78cf870e7fbbd9d792a2a9`.
- All 17 assets are uploaded to draft tag `1.7.4`: nine packages, three blockmaps, four updater metadata files, and SHA256SUMS. Complete verification passed; remote sizes/digests match local files. The current release body is the user's version with only its Mac pending line replaced by the correct link. Do not overwrite it with an earlier local draft.
- Mac notarization, stapler, Gatekeeper, packaged desktop/CLI/MCP and all 12 focused native Mac UI tests passed. No additional Mac foreground run is needed. The original Babu suite passed 95/96; the unchanged full eight-lap test passed its 120-second rerun, covering all 96 tests. Node regressions and native Windows/Linux package gates passed.
- Release packaging worktree: `/private/tmp/signboard-174-dist-20260921`, detached at `6b727a1`. Its `dist/` contains every release file and SHA256SUMS. Later release commits only update documentation/validation/ignore rules; `npm run build:check` still passes.

## Remaining release gate

Babu is temporarily advertised as `babu-2.local`, observed at `192.168.68.77`. Its ED25519 identity matched the saved `babu.local` key. Connect with `ssh -o BatchMode=yes -o StrictHostKeyChecking=yes -o HostKeyAlias=babu.local cdevroe@babu-2.local`; use hostname discovery and verify keys again if this changes. Do not use an unverified historical address. The private runner SSH wrapper is `/private/tmp/signboard-174-ssh/ssh`.

The exact Linux x64 release AppImage is extracted under `/home/cdevroe/.local/share/signboard-lab/release-candidate-1.7.4-20260922`. It passed its fresh packaged gate and two-minute smoke. The two-hour soak started at `2026-09-22T11:36:37.433Z`, expected to finish around `13:36:37Z` (9:37 a.m. EDT). The transient user service is `signboard-174-candidate-soak`, bounded to 135 minutes, private Xvfb/Openbox, disposable data, serialized through the usual Playwright lock. Check `soak/result.json`, `soak/observations.jsonl`, `run.log`, and service status. The wrapper runs the existing `scripts/soak-maintenance.js`; it does not modify normal profiles or the development symlink. While running, avoid competing Babu builds/tests. If it fails, investigate and keep the release draft.

After success, retrieve the result and resource samples, verify actual two-hour duration, operations/content assertions, and resource trends. Record completed evidence in `docs/research/1.7.4-validation/` and revise the preparation record so its final status is clear. Commit and push that documentation, check runtime stamp, and update the PR validation text. Recheck PR head, mergeability, and release assets/body. Mark PR #60 ready and merge with a normal merge commit so Marcus's co-authored commit survives. Pull main, set draft tag `1.7.4` target to the resulting main commit, then publish the existing release without replacing its body. Verify public release/tag/assets and that both contributor issues closed. Refresh Babu's release baseline from the already hash-verified AppImage, preserving its previous release artifact and normal profile; follow the lab runbook.

## Prepared 2.0 integration

Detached worktree `/private/tmp/signboard-174-beta-integration` contains prepared merge commit `4679deb7eb125f651dacb7395a6020b769d20377`, parents `deaaef0` (the current local `beta/2.0.0`) and `e046a89` (release sources). It retains all 2.0-specific features and adds 1.7.4. It has development stamp `20260922.1`, fingerprint `6b0496480d40b2e13da1e7e5aba356af6f8d311b03a4203c2541e5d46fddc869`. Its `node_modules` is a symlink to the main checkout's existing 2.0 dependencies; do not package that symlink into an app. The transfer runner stages allowlisted sources only. See its `docs/research/2.0.0-forward-port-1.7.4-2026-09-22.md` for conflict decisions and 22 passing Node suites; logs/results are `/private/tmp/signboard-174-beta-node-tests.log` and `...-results.json`. A new delayed-response appearance regression passed separately after the suite.

Once the soak is over, run full isolated Electron tests for this prepared checkout with `PATH="/private/tmp/signboard-174-ssh:$PATH" SIGNBOARD_PLAYWRIGHT_HOST=cdevroe@babu-2.local npm run test:playwright`. Address genuine integration failures while preserving 2.0 behavior. Stamp again only if runtime/build sources change, and validate any new stamp. After publishing 1.7.4 and pulling main, switch the main checkout to `beta/2.0.0`, verify its tip has not moved unexpectedly, fast-forward to the validated prepared integration, and merge final main (which adds release evidence/ancestry). If beta moved concurrently, integrate rather than resetting it. Commit the final merge; do not push this previously local-only beta branch without a reason to publish it.

Refresh and validate a separate 2.0 Linux development package on Babu, including packaged desktop/CLI/MCP/TUI and interactive TUI smoke, then promote `development-current` only after passing. Preserve the old build. Follow `/Users/cdevroe/Sites/projects/Omarchy-Admin/AGENTS.md` and `docs/signboard-lab.md`; use a private desktop and temporary profiles. No production OS/display/network changes are authorized by this release task.

Restore the user's exact saved checklist edit on the actual beta checkout using stash object `bde66fcaecd5b07c9220d0479fd3e95e9e6e72d8` (named `Preserve 2.0 live-mode checklist while trying 1.7.4`). It only modifies `docs/signboard-pro/LIVE_MODE_CHECKLIST.md`; expected restored file SHA256 is `f2434d7af2d33cb241fd5962435fd1dd32f5c4e5b90d12fabb3e6a75731b6145`. Apply, do not blindly pop/drop it. Leave the user's edit uncommitted and identify it clearly in the final answer. The independent `/private/tmp/signboard-pro-checkout` worktree belongs to other work; do not alter it.

Stop the continuation heartbeat after successful publication/integration. Notify only on a meaningful change, failure needing action, or completion; remain quiet while the soak is healthy and still running. Never claim full completion before the release is public and beta is integrated/validated.
